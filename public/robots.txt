# Allow all user agents to crawl the site
User-agent: *
Allow: /

# Disallow crawling of specific paths if needed later
# Disallow: /admin/
# Disallow: /api/

# Sitemap location (update if URL changes)
Sitemap: [YOUR_SITE_URL]/sitemap.xml
